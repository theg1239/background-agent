{
  "name": "@background-agent/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "NODE_ENV=production node dist/index.js",
    "build": "tsup src/index.ts --format esm --dts false --splitting false --target node20"
  }
}
